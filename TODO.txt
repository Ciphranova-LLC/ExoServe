Re-implement file size in UI
Re-implement sorting by file size or by name

Display progress bar when uploading files

Option to delete a folder or file when right clicking it

Decide the destination on client-side in a way that interacting with the UI doesn't change the destination

Protect the keyfile with a password

